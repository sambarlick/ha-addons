name: "Fing Agent Home Assistant Add-on"
description: "Runs the official fing/fing-agent container"
version: "0.3.6"
slug: "fing_agent"

# 'build_from:' is GONE. 'build.json' handles this now.

# --- Network Settings (CRITICAL) ---
host_network: true

# --- Privileges (CRITICAL) ---
privileged:
  - NET_ADMIN
  - SYS_ADMIN 

# --- Persistent Data ---
map: ["data:rw"] 

# --- Architecture Support ---
arch:
  - aarch64
  - amd64
  - armv7
  
init: false 
startup: "application"
boot: "auto"

# --- Professional Section ---
documentation: "https://github.com/sambarlick/ha-addons/blob/main/fing_agent/README.md"
support: "https://github.com/sambarlick/ha-addons/issues"
url: "https://www.fing.com"
codeowners: ["@sambarlick"]
